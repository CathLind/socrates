<main class="page-main page-main-login" role="main">
  {{ page.help }}

  <div class="login-container">
    {{ page.top }}

    <div class="left-col">
      {{ page.content }}
	   
    </div>

    <div class="right-col">
      <div id="user-sidebar">
        {{ page.menu }}

        {% block valkommen %}
		  <div class="valkommen">
            <h4>Vi bygger en värld av kunskap</h4>
            <p>Under de 25 år InköpsForum har funnits har vi samlat på oss kunskap i olika former. Nu börjar vi publicera den. Bakom väggen hittar du både material och gemenskapen med andra inköpare. </p>
            <p>Välkommen! Gå på upptäcksfärd, det kostar ingenting.</p>
		  </div>
        {% endblock %}

        {% block content %}
        <div class="form-wrapper user-login content-box" data-target="{{ path('user.login') }}"
          {% if route_name == 'user.pass' or route_name == 'user.register' %}style="display: none;"{% endif %}>
          <h4>{{ 'LOGGA IN PÅ INKÖPSFORUM'|t }}</h4>
          {{ login_form }}

  <div class="status_messages">
            {{ page.status_messages }}
          </div>

          {% if opigno_demo != TRUE %}
            <div class="switch-link switch-link-bottom text-center">
              {% if register != 'admin_only' %}
                <div><a href="{{ path('user.register') }}">{{ 'Create new account'|t }}</a></div>
              {% endif %}
            </div>
          {% endif %}
        </div>

        <div class="form-wrapper user-password" data-target="{{ path('user.pass') }}"
          {% if route_name != 'user.pass' %}style="display: none;"{% endif %}>

          <h1>{{ 'REQUEST NEW PASSWORD'|t }}</h1>

          <div class="subtitle">{{ 'Enter your email address below and we\'ll'|t }}
            <br>
            {{ 'send you password reset instructions.'|t }}
          </div>

          {{ password_form }}
          
           <div class="switch-link switch-link-bottom text-center">
            <div><a href="{{ path('user.login') }}">{{ 'Login'|t }}</a></div>

            {% if register != 'admin_only' %}
              <div><a href="{{ path('user.register') }}">{{ 'Create new account'|t }}</a></div>
            {% endif %}
          </div>

          <div class="status_messages">
            {{ page.status_messages }}
          </div>

        </div>

        {% if register != 'admin_only' %}
          <div class="form-wrapper user-register" data-target="{{ path('user.register') }}"
            {% if route_name != 'user.register' %}style="display: none;"{% endif %}>

            <h1>{{ 'CREATE NEW ACCOUNT'|t }}</h1>

            {{ register_form }}

            <div class="status_messages">
              {{ page.status_messages }}
            </div>

            <div class="switch-link switch-link-bottom text-center">
              <div><a href="{{ path('user.login') }}">{{ 'Login'|t }}</a></div>
            </div>
          </div>
        {% endif %}
        {% endblock %}

        
		<div id="footer-block">
          {{ page.footer_first }}
          {{ page.footer_second }}
          {{ page.footer_third }}
        </div>
        
        <div id="footer">
          {{ page.footer }}
        </div>
      </div>
      <div id="sidebar_second">
        {{ drupal_view('annonser', 'block_1') }}
      </div>
    </div>
  </div>
</main>
